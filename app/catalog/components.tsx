import type {Product} from '@/lib/catalog';
import {hasProductPhoto,productPrice} from '@/lib/catalog';
import {ProductVisual} from '../product-visual';
import {catalogSections,categoryPath,productPath} from '@/lib/seo';

export function SectionLinks({current}:{current?:string}){return <nav className="catalog-section-links" aria-label="Разделы каталога"><a href="/catalog" aria-current={!current?'page':undefined}>Все товары</a>{catalogSections.map(s=><a key={s.slug} href={categoryPath(s.name)} aria-current={s.name===current?'page':undefined}>{s.name}</a>)}</nav>}
export function ProductCards({products}:{products:Product[]}){return <div className="product-grid">{products.map(p=><article className="product-card" key={p.id}><a className="product-image" href={productPath(p)} aria-label={p.title}><ProductVisual product={p} width="400" height="400" loading="lazy"/></a><div className="product-info"><div className="product-brand">{p.brand||p.category}<span>{p.series}</span></div><a href={productPath(p)} className="product-name">{p.title}</a><p className="specification">{p.specification}</p><div className="product-bottom"><div><strong>{productPrice(p)}</strong><p className="catalog-price-note">{p.demo?'Ориентир · цена и наличие требуют подтверждения':'Наличие и условия заказа уточняйте'}</p></div></div><a className="source-link" href={productPath(p)}>{hasProductPhoto(p.image)?'Характеристики и фото →':'Характеристики →'}</a></div></article>)}</div>}
