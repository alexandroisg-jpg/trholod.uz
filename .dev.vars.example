# Local-only values. Generate your own secrets; never commit .dev.vars.
# The published Worker uses encrypted runtime secrets in the owner's Cloudflare.
ADMIN_ACCESS_KEY_SHA256=
SESSION_SECRET=
OWNER_EMAIL=owner@example.com
